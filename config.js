const config = {
  timeUntilRedirect: 10000, // Время до редиректа
  redirectURL: "https://seller.imetastore.io/login",
  redirectFirst: 2000, // Ссылка на редирект
  shouldValidateInputs: true
};

export default config;
